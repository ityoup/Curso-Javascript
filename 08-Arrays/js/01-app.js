// Manera mas usada para crear arreglos
const numeros = [10, 20, 30];

// Manera no tan comun de declarar arreglos
const meses = new Array("Enero", "Febrero");

console.log(numeros);
console.log(meses);


// se pueden crear arreglos con datos de todo tipo
const deTodo = ["Hola", 20, true, null, {nombre: "Juan", trabajo: "programador"}, [2,3,3]];
console.log(deTodo);