// Acceder a un objeto
const persona = {
    nombre: "Juan",
    edad: 21,
    hobbie: "Programar"
}

console.log(persona);

//Podemos acceder una variable del objeto con un punto.
console.log(persona.nombre);

//tambien se usa esta, casi no se usa, pero se debe de conocer
console.log(persona["nombre"]);